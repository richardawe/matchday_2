import type {Metadata} from "next";
import Link from "next/link";
import {fallbackFixtures,warriorImage} from "../../growth-data";
import ShareChallenge from "../../ShareChallenge";
const find=(slug:string)=>fallbackFixtures.find(f=>f.slug===slug);
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,f=find(slug);if(!f)return{title:"Match not found"};const title=`${f.home} v ${f.away} — Matchday Myth`;return{title,description:f.myth,alternates:{canonical:`/match/${slug}`},openGraph:{title,description:f.myth,type:"article",images:[warriorImage(f.home)]},twitter:{card:"summary_large_image",title,description:f.myth,images:[warriorImage(f.home)]}}}
export default async function MatchPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params,f=find(slug);if(!f)return <main className="growthPage"><h1>Battle not found.</h1><Link href="/">Return to Matchday</Link></main>;const date=new Intl.DateTimeFormat("en-GB",{weekday:"long",day:"numeric",month:"long",hour:"2-digit",minute:"2-digit",timeZone:"Europe/London"}).format(new Date(f.kickoff));return <main className="growthPage"><Link className="brand" href="/"><span>MD</span>MATCHDAY</Link><p className="eyebrow">MATCHWEEK {String(f.matchweek).padStart(2,"0")} · {f.status}</p><h1>{f.home}<i>VS</i>{f.away}</h1><p className="kickoffDate">{date}</p><div className="growthWarriors"><img src={warriorImage(f.home)} alt={`${f.home} warrior`}/><b>VS</b><img src={warriorImage(f.away)} alt={`${f.away} warrior`}/></div><blockquote>{f.myth}</blockquote><ShareChallenge fixtureId={f.id} title={`${f.home} v ${f.away}`} playUrl={`/?fixture=${f.id}#game`}/><p className="rightsNote">Unofficial fan-made visual mythology. Not affiliated with or endorsed by any club or league.</p><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({"@context":"https://schema.org","@type":"SportsEvent",name:`${f.home} v ${f.away}`,startDate:f.kickoff,eventStatus:"https://schema.org/EventScheduled",url:`/match/${slug}`,homeTeam:{"@type":"SportsTeam",name:f.home},awayTeam:{"@type":"SportsTeam",name:f.away}})}}/></main>}
