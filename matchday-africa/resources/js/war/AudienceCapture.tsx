"use client";
import {useState} from "react";
export default function AudienceCapture(){const [email,setEmail]=useState(""),[done,setDone]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();await fetch("/war/api/growth/subscribe",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});setDone(true)};return <section className="audienceCapture"><p className="eyebrow">THE WEEKLY WAR COUNCIL</p><h2>Never miss the next campaign.</h2><p>Get the three hero clashes, friend challenges and Myth Grammar results each week.</p>{done?<b>YOU HAVE JOINED THE COUNCIL.</b>:<form onSubmit={submit}><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Your email"/><button>JOIN FREE</button></form>}<small>One weekly email plus optional kickoff alerts. Unsubscribe anytime.</small></section>}
