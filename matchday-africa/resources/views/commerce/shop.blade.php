@extends('layouts.public') @section('content')
<section class="md-phase md-commerce"><div class="md-wrap"><p class="md-eyebrow">THE QUARTERMASTER</p><h1>Original goods. Built for the faithful.</h1><p class="md-lead">Every item uses Matchday Africa’s original, rights-safe warrior artwork—no club badges or protected marks.</p><div class="md-product-grid">@foreach($products as $product)<article class="md-product"><img src="{{ asset(ltrim($product->image,'/')) }}" alt="{{ $product->name }}"><div><span>{{ strtoupper($product->type) }}</span><h2>{{ $product->name }}</h2><p>{{ $product->description }}</p><div class="md-buy"><strong>£{{ number_format($product->price/100,2) }}</strong>@auth<form method="POST" action="{{ route('commerce.checkout',$product) }}">@csrf @if(request('creator'))<input type="hidden" name="creator" value="{{ request('creator') }}">@endif<button class="md-primary">{{ $product->type==='digital'?'Unlock':'Order now' }}</button></form>@else<a class="md-primary" href="{{ route('login') }}">Sign in to buy</a>@endauth</div></div></article>@endforeach</div></div></section>
@endsection
