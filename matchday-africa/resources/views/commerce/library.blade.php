@extends('layouts.public') @section('content')<section class="md-phase"><div class="md-wrap"><p class="md-eyebrow">MY DIGITAL VAULT</p><h1>Your unlocked collection.</h1><div class="md-product-grid">@forelse($entitlements as $unlock)<article class="md-product"><img src="{{ asset(ltrim($unlock->product->image,'/')) }}" alt=""><div><h2>{{ $unlock->product->name }}</h2><p>Unlocked {{ $unlock->granted_at->format('j M Y') }}</p><a class="md-primary" href="{{ route('library.download',$unlock->product) }}">Download ZIP</a></div></article>@empty<div class="md-panel"><p>No unlocks yet.</p><a class="md-primary" href="{{ route('shop.index') }}">Visit the shop</a></div>@endforelse</div></div></section>@endsection
