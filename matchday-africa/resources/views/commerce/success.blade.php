@extends('layouts.public') @section('content')<section class="md-phase"><div class="md-wrap md-narrow"><p class="md-eyebrow">ORDER RECEIVED</p><h1>The quartermaster has your order.</h1>@if($order)<div class="md-panel"><p>Order #{{ $order->id }} · {{ strtoupper($order->status) }}</p>@foreach($order->items as $item)<h2>{{ $item->product_name }}</h2>@endforeach @if($order->status==='paid')<a class="md-primary" href="{{ route('library.index') }}">Open my library</a>@else<p>Payment confirmation can take a few seconds. Refresh this page shortly.</p>@endif</div>@else<p>We are waiting for Stripe to confirm this order.</p>@endif</div></section>@endsection
