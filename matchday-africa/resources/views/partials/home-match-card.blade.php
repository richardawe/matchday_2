<a class="md-pulse-match" href="{{ route('matches.show', $match) }}"><small>{{ $match->league?->name ?? 'Football' }} · @if($mode === 'live') LIVE @elseif($mode === 'upcoming') {{ $match->match_date->isToday() ? 'TODAY' : Str::upper($match->match_date->format('D j M')) }} @else FINAL @endif</small><div><span>{{ $match->homeTeam?->name ?? 'Home' }}</span><b>@if($mode === 'upcoming'){{ $match->match_date->format('H:i') }}@else{{ $match->home_score ?? '–' }}—{{ $match->away_score ?? '–' }}@endif</b><span>{{ $match->awayTeam?->name ?? 'Away' }}</span></div></a>
