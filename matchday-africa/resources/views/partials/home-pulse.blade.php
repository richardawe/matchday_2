<div class="md-pulse-grid">
    <div class="md-pulse-column md-live-column"><div class="md-pulse-title"><span class="md-pulse-dot"></span><b>LIVE NOW</b><small>Updated {{ now()->format('H:i:s') }}</small></div>@forelse($liveMatches as $match) @include('partials.home-match-card', ['match'=>$match,'mode'=>'live']) @empty <div class="md-pulse-empty"><strong>No live matches right now.</strong><span>Live status appears only after a recent provider update.</span></div> @endforelse</div>
    <div class="md-pulse-column"><div class="md-pulse-title"><b>COMING UP</b><small>Next 7 days</small></div>@forelse($upcomingMatches->take(4) as $match) @include('partials.home-match-card', ['match'=>$match,'mode'=>'upcoming']) @empty <div class="md-pulse-empty"><strong>Nothing scheduled yet.</strong><span>The fixture service will check again automatically.</span></div> @endforelse</div>
    <div class="md-pulse-column"><div class="md-pulse-title"><b>RESULTS</b><small>Last 48 hours</small></div>@forelse($recentResults->take(4) as $match) @include('partials.home-match-card', ['match'=>$match,'mode'=>'result']) @empty <div class="md-pulse-empty"><strong>No recent results.</strong><span>Confirmed final scores will appear here.</span></div> @endforelse</div>
</div>
