@extends('layouts.public')
@section('content')
<section class="md-news-hero"><div class="md-wrap"><p class="md-eyebrow">MATCHDAY AFRICA NEWSROOM</p><h1>The game,<br><em>properly told.</em></h1><p>Fresh football reporting, match intelligence and stories selected for fans across Africa.</p></div></section>
<section class="md-news-body"><div class="md-wrap"><header class="md-dash-heading"><div><p class="md-eyebrow">LATEST DISPATCHES</p><h2>From the newsroom</h2></div><a href="{{ route('matches.index') }}">Open match centre →</a></header>
<div class="md-news-grid">@forelse($blogs as $blog)<article class="{{ $loop->first&&$blogs->currentPage()===1?'is-lead':'' }}"><a class="md-news-image" href="{{ route('blogs.show',$blog) }}"><img src="{{ $blog->featured_image_url }}" alt="{{ $blog->title }}" loading="{{ $loop->first?'eager':'lazy' }}"></a><div><small>{{ $blog->formatted_published_date }} · {{ $blog->reading_time }}</small><h2><a href="{{ route('blogs.show',$blog) }}">{{ $blog->title }}</a></h2><p>{{ Str::limit(strip_tags($blog->excerpt),$loop->first?240:150) }}</p><a class="md-news-read" href="{{ route('blogs.show',$blog) }}">Read the story →</a></div></article>@empty<div class="md-pred-empty"><span>90′</span><div><h3>The newsroom is preparing the next edition.</h3><p>Return shortly for fresh football stories.</p></div></div>@endforelse</div>
@if($blogs->hasPages())<div class="md-pred-pages">{{ $blogs->links() }}</div>@endif</div></section>
@endsection
