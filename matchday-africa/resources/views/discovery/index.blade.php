@extends('layouts.public') @section('content')
<section class="md-phase"><div class="md-wrap"><p class="md-eyebrow">THE CONTINENT ABROAD & AT HOME</p><h1>African player atlas</h1><p class="md-lead">Discover the players carrying African football across leagues and borders.</p><form class="md-discovery-filter"><input name="search" value="{{ request('search') }}" placeholder="Search a player"><select name="country"><option value="">All nations</option>@foreach($countries as $c)<option value="{{ $c->nationality_code }}" {{ request('country')===$c->nationality_code?'selected':'' }}>{{ $c->nationality }}</option>@endforeach</select><button class="md-primary">Explore</button></form>
<div class="md-player-grid">@forelse($players as $player)<a href="{{ route('discovery.show',$player) }}" class="md-player-card">@if($player->photo_url)<img src="{{ $player->photo_url }}" alt="{{ $player->name }}">@else<div class="md-player-placeholder">{{ strtoupper(substr($player->name,0,2)) }}</div>@endif<div><small>{{ $player->nationality_flag }} {{ $player->nationality }}</small><h2>{{ $player->name }}</h2><p>{{ $player->position }} · {{ $player->team?->name }}</p></div></a>@empty<div class="md-panel">No African players match this search.</div>@endforelse</div>{{ $players->links() }}</div></section>@endsection
