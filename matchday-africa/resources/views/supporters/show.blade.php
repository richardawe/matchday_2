@extends('layouts.public') @section('content')
<section class="md-phase"><div class="md-wrap md-narrow"><p class="md-eyebrow">SUPPORTER RECORD</p><h1>{{ '@'.($user->username ?: Str::slug($user->name)) }}</h1><p class="md-lead">{{ $user->bio ?: 'A supporter standing beneath the Matchday Africa banner.' }}</p><div class="md-stat-row"><span><b>{{ $stats['points'] }}</b>PREDICTION PTS</span><span><b>{{ $user->matchday_points }}</b>MATCHDAY PTS</span><span><b>{{ $user->best_streak }}</b>BEST STREAK</span><span><b>{{ $stats['leagues'] }}</b>LEAGUES</span></div><h2 class="md-subhead">Badges</h2><div class="md-badges">@forelse($user->badges as $badge)<span><b>✦</b>{{ $badge->label }}</span>@empty<p>No badges claimed yet.</p>@endforelse</div>@if($user->creatorProfile?->status==='approved')<a class="md-secondary" href="{{ route('creators.show',$user->creatorProfile) }}">Read creator stories</a>@endif</div></section>@endsection
