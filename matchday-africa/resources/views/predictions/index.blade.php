@extends('layouts.app')
@section('content')
<x-sponsor-slot slot="predictions" />
<section class="md-pred-hero"><div class="md-wrap"><div><p class="md-eyebrow">MATCHDAY PREDICTIONS</p><h1>Call it before<br><em>kickoff.</em></h1><p>Pick the scores, climb the table and prove you saw the result coming.</p></div><div class="md-pred-hero-actions"><a class="md-primary" href="#challenges">Make your calls</a><a class="md-secondary" href="{{ route('predictions.leaderboard') }}">View leaderboard</a></div></div></section>
<section class="md-pred-stats"><div class="md-wrap"><div><span>POINTS</span><strong>{{ number_format($userStats['total_points']) }}</strong><small>Your all-time score</small></div><div><span>CALLS MADE</span><strong>{{ number_format($userStats['total_predictions']) }}</strong><small>Across every challenge</small></div><div><span>RIGHT CALLS</span><strong>{{ number_format($userStats['correct_predictions']) }}</strong><small>Correct predictions</small></div><div><span>ACCURACY</span><strong>{{ $userStats['accuracy_percentage'] }}%</strong><small>Your current strike rate</small></div></div></section>
<section id="challenges" class="md-pred-body"><div class="md-wrap"><div class="md-pred-heading"><div><p class="md-eyebrow">OPEN PREDICTION ROUNDS</p><h2>What happens next?</h2></div><div><a href="{{ route('predictions.history') }}">My history</a><a href="{{ route('groups.index') }}">Private leagues</a></div></div>
@if($predictionSets->count() > 0)<div class="md-challenge-grid">@foreach($predictionSets as $predictionSet) @php($userPredictionStats = $predictionSet->userPredictions()->where('user_id', auth()->id())->get()) @php($hasCalls = $userPredictionStats->isNotEmpty()) @php($isClosed = $predictionSet->isDeadlinePassed())
<article class="md-challenge-card {{ $isClosed ? 'is-closed' : '' }}"><header><span>{{ $isClosed ? 'CLOSED' : 'OPEN NOW' }}</span><small>{{ $predictionSet->prediction_deadline->diffForHumans() }}</small></header><div class="md-challenge-copy"><h3>{{ $predictionSet->name }}</h3><p>{{ $predictionSet->description ?: 'Make your calls before the deadline and collect points when the results land.' }}</p></div><div class="md-challenge-meta"><div><strong>{{ $predictionSet->matches->count() }}</strong><span>Matches</span></div><div><strong>{{ number_format($predictionSet->getParticipationCount()) }}</strong><span>Players</span></div><div><strong>{{ $userPredictionStats->sum('points_earned') }}</strong><span>Your points</span></div></div><footer><small>Deadline · {{ $predictionSet->prediction_deadline->format('D j M, H:i') }}</small><a href="{{ route('predictions.show', $predictionSet) }}">{{ $isClosed ? 'View results' : ($hasCalls ? 'Update calls' : 'Make predictions') }} →</a></footer></article>@endforeach</div>
@else<div class="md-pred-empty"><span>90′</span><div><h3>The next challenge is being prepared.</h3><p>Fresh prediction sets will appear here as upcoming fixtures are published.</p><a href="{{ route('matches.index') }}">Browse upcoming matches →</a></div></div>@endif</div></section>
<section class="md-pred-recent"><div class="md-wrap"><div class="md-pred-heading"><div><p class="md-eyebrow">YOUR FORM</p><h2>Recent calls.</h2></div><a href="{{ route('predictions.history') }}">Full prediction history →</a></div><div class="md-recent-calls">@forelse($recentPredictions ?? [] as $prediction)<a href="{{ route('matches.show', $prediction->match) }}" class="md-recent-call"><span class="{{ $prediction->is_correct === true ? 'won' : ($prediction->is_correct === false ? 'lost' : 'pending') }}">{{ $prediction->is_correct === true ? '✓' : ($prediction->is_correct === false ? '×' : '·') }}</span><div><small>{{ $prediction->match->league?->name }} · {{ ucfirst(str_replace('_', ' ', $prediction->prediction_type)) }}</small><strong>{{ $prediction->match->homeTeam->name }} <i>v</i> {{ $prediction->match->awayTeam->name }}</strong><p>Your call: {{ $prediction->prediction_value }}@if($prediction->is_correct !== null) · Final: {{ $prediction->match->home_score }}–{{ $prediction->match->away_score }}@endif</p></div><b>{{ $prediction->is_correct === null ? 'Pending' : '+'.$prediction->points_earned.' pts' }}</b></a>@empty<div class="md-pred-empty"><span>0</span><div><h3>No calls yet.</h3><p>Enter an open challenge to start building your record.</p></div></div>@endforelse</div></div></section>
@endsection
