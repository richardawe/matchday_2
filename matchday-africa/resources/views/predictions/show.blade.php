@extends('layouts.app')
@section('content')
@php($saved=$userPredictions->keyBy(fn($p)=>$p->match_id.'|'.$p->prediction_type))
<section class="md-pred-subhero"><div class="md-wrap"><div><p class="md-eyebrow">PREDICTION ROUND</p><h1>{{ $prediction->name }}</h1><p>{{ $prediction->description ?: 'Make every call before kickoff. Your points feed the same global and private-league tables.' }}</p></div><div class="md-round-clock"><span>{{ $prediction->isDeadlinePassed() ? 'CLOSED' : 'LOCKS IN' }}</span><strong>{{ $prediction->prediction_deadline->format('D d M · H:i') }}</strong><small>{{ $prediction->prediction_deadline->diffForHumans() }}</small></div></div></section>
<nav class="md-pred-nav" aria-label="Prediction navigation"><div class="md-wrap"><a href="{{ route('predictions.index') }}">Rounds</a><a href="{{ route('predictions.history') }}">My history</a><a href="{{ route('predictions.leaderboard') }}">Leaderboard</a><a href="{{ route('groups.index') }}">Private leagues</a></div></nav>
<section class="md-round-body"><div class="md-wrap md-round-layout"><main>
@if(session('success'))<div class="md-alert-success" role="status">{{ session('success') }}</div>@endif
@if($errors->any())<div class="md-alert-error" role="alert"><strong>Check your calls:</strong><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<form method="POST" action="{{ $hasSubmitted ? route('predictions.update',$prediction) : route('predictions.submit',$prediction) }}" class="md-round-form">@csrf @if($hasSubmitted)@method('PUT')@endif<input type="hidden" name="_force_redirect" value="1">
@forelse($prediction->matches as $item) @php($match=$item->match) @php($value=old('predictions.'.$item->id.'.prediction_value',$saved->get($match->id.'|'.$item->prediction_type)?->prediction_value))
<fieldset class="md-call-card" @disabled($prediction->isDeadlinePassed())><legend><span>{{ $match->league?->name ?? 'Football' }} · {{ $match->match_date->format('D d M · H:i') }}</span><strong>{{ $match->homeTeam?->name }} <i>v</i> {{ $match->awayTeam?->name }}</strong><small>{{ $item->points_value }} {{ Str::plural('point',$item->points_value) }} · {{ ucfirst(str_replace('_',' ',$item->prediction_type)) }}</small></legend>
<input type="hidden" name="predictions[{{ $item->id }}][match_id]" value="{{ $match->id }}"><input type="hidden" name="predictions[{{ $item->id }}][prediction_type]" value="{{ $item->prediction_type }}">
@if($item->prediction_type==='result')<div class="md-choice-grid">@foreach(['Home Win'=>$match->homeTeam?->name.' win','Draw'=>'Draw','Away Win'=>$match->awayTeam?->name.' win'] as $option=>$label)<label><input type="radio" name="predictions[{{ $item->id }}][prediction_value]" value="{{ $option }}" @checked($value===$option) required><span>{{ $label }}</span></label>@endforeach</div>
@elseif($item->prediction_type==='score')<label class="md-call-input"><span>Exact score (home-away)</span><input name="predictions[{{ $item->id }}][prediction_value]" value="{{ $value }}" inputmode="numeric" pattern="\d{1,2}-\d{1,2}" placeholder="2-1" required></label>
@elseif($item->prediction_type==='goalscorer')<label class="md-call-input"><span>First goalscorer</span><input name="predictions[{{ $item->id }}][prediction_value]" value="{{ $value }}" placeholder="Player name" required></label>
@else<label class="md-call-input"><span>Total goals</span><select name="predictions[{{ $item->id }}][prediction_value]" required><option value="">Choose a line</option>@foreach(['Over 0.5','Over 1.5','Over 2.5','Over 3.5','Under 0.5','Under 1.5','Under 2.5','Under 3.5'] as $option)<option @selected($value===$option)>{{ $option }}</option>@endforeach</select></label>@endif
</fieldset>@empty<div class="md-pred-empty"><span>0</span><div><h3>No fixtures in this round.</h3><p>The prediction desk is preparing the next calls.</p></div></div>@endforelse
@if($prediction->matches->isNotEmpty()&&!$prediction->isDeadlinePassed())<button class="md-primary md-round-submit">{{ $hasSubmitted ? 'Update my predictions' : 'Submit my predictions' }}</button>@endif</form></main>
<aside class="md-dash-rail"><p class="md-eyebrow">ONE SYSTEM</p><h2>Every call counts.</h2><p>Your result, score and goals calls all earn points in one record.</p><a href="{{ route('predictions.leaderboard') }}"><span>01</span><div><strong>Global table</strong><small>Everyone, one ranking.</small></div><b>→</b></a><a href="{{ route('groups.index') }}"><span>02</span><div><strong>Private leagues</strong><small>The same points among friends.</small></div><b>→</b></a><a href="{{ route('predictions.history') }}"><span>03</span><div><strong>Your record</strong><small>Every settled and pending call.</small></div><b>→</b></a></aside></div></section>
@endsection
